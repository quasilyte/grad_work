project(lexer)
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++14")
set(lexer_src char_type.hpp lexer.hpp token.hpp)
add_library(lexer STATIC ${lexer_src})
set_target_properties(lexer PROPERTIES LINKER_LANGUAGE CXX)
